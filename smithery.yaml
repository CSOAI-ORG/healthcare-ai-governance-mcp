<<<<<<< c45e0206c9498dd4f7a9714bc02410ebd0babd30
# smithery.yaml — Smithery (https://smithery.ai) submission payload
# Repo: CSOAI-ORG/healthcare-ai-governance-mcp
# Generated: 2026-06-09
# Transport: stdio (PyPI package, installed via uvx)
# See: DISTRIBUTION_GAPS_2026-06-08.md for the channel strategy
startCommand:
  type: stdio
  timeout: 120000
  pingInterval: 5000
  pingTimeout: 30000
  configSchema:
    type: object
    required: []
    properties:
      apiKey:
        type: string
        description: "MEOK API key (optional; required for x402 paywalled tools)"
        default: ""
      x402PayTo:
        type: string
        description: "USDC address for x402 micropayments (default: meok.ai wallet)"
        default: ""
      logLevel:
        type: string
        description: "Log level (debug, info, warn, error)"
        default: "info"
    additionalProperties: false
  commandFunction: |-
    (config) => ({
      command: 'uvx',
      args: ['healthcare_ai_governance'],
      env: {
        MEOK_API_KEY: config.apiKey || '',
        X402_PAY_TO: config.x402PayTo || '',
        MEOK_LOG_LEVEL: config.logLevel || 'info',
      }
    })
=======
name: healthcare-ai-governance-mcp
description: Healthcare Ai Governance automation via MCP. Includes classify samd,
  check cds exemption, hipaa ai check. By MEOK AI Labs.
version: 1.0.0
tools:
- name: classify_samd
  description: Classify AI/ML Software as Medical Device (SaMD) per FDA framework.
  parameters:
  - name: device_description
    type: string
    required: true
  - name: intended_use
    type: string
    required: true
  - name: risk_to_patient
    type: string
    required: false
- name: check_cds_exemption
  description: Check if Clinical Decision Support AI qualifies for FDA CDS exemption.
  parameters:
  - name: function_description
    type: string
    required: true
  - name: provides_diagnosis
    type: boolean
    required: true
  - name: requires_professional
    type: boolean
    required: true
- name: hipaa_ai_check
  description: Check HIPAA compliance for AI systems processing health data.
  parameters:
  - name: data_types
    type: string
    required: true
  - name: processing_purpose
    type: string
    required: true
  - name: has_baa
    type: boolean
    required: false
- name: who_health_ai_ethics
  description: Evaluate against WHO's 6 principles for health AI ethics.
  parameters:
  - name: ai_application
    type: string
    required: true
- name: dual_compliance_check
  description: Check dual FDA + EU AI Act compliance for medical AI devices.
  parameters:
  - name: description
    type: string
    required: true
  - name: jurisdictions
    type: string
    required: false
startCommand:
  type: stdio
  configSchema:
    type: object
    required:
    - apiKey
    properties:
      apiKey:
        type: string
        description: Your MEOK API Key
  commandFunction: "(config) => ({\n  command: 'uvx',\n  args: ['healthcare-ai-governance-mcp',\
    \ '--api-key', config.apiKey],\n  env: {\n    MEOK_API_KEY: config.apiKey\n  }\n\
    })"
>>>>>>> feat/smither-workflows
